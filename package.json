{
  "name": "skanjalkar-portfolio",
  "version": "0.1.0",
  "description": "Tailwind CSS for Leptos portfolio",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./styles/input.css -o ./styles/output.css --minify",
    "watch:css": "tailwindcss -i ./styles/input.css -o ./styles/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}